package clock;


import static org.junit.Assert.*;
import org.junit.Test; 


public class TestRunner {
	
	private Clock clock = new Clock();
	
		
	
	public void test1() {
		Clock clock = new Clock();
		String result = clock.ready();
		assertEquals("CHANGE_TIME", result);
		}
	}


